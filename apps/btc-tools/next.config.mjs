/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server port configuration
  // This will be used when running: npm run dev
  // To run on a specific port: PORT=3001 npm run dev
};

export default nextConfig;

